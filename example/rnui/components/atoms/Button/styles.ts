import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    button: {
        fontSize: 17,
    },

    buttonSubmit: {
        fontWeight: "500",
    },

    buttonDisabled: {
        color: "#007afe",
        opacity: 0.5,
    },

    buttonActive: {
        color: "#007afe",
    },
});
