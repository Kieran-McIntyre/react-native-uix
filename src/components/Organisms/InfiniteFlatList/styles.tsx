import { StyleSheet } from "react-native";

export const dynamicStyles = (): StyleSheet.NamedStyles<any> => ({
  loadingFooter: {
    minHeight: 50,
    alignItems: "center",
    justifyContent: "center",
  },
});
